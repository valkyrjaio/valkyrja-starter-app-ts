{
  "private": true,
  "type": "module",
  "scripts": {
    "check": "vitest run",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.11",
    "esbuild": "^0.28.2",
    "vitest": "^4.1.11"
  }
}
